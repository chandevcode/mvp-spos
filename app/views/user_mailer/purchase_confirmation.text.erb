Thank you for your purchase, <%= @user.email %>!

Your order #<%= @transaction.id %> has been completed successfully.

Order Summary:
<% @items.each do |item| %>
  - <%= item.product.name %> x <%= item.quantity %> @ <%= number_to_currency(item.price) %> = <%= number_to_currency(item.price * item.quantity) %>
<% end %>

Total: <%= number_to_currency(@transaction.total_price) %>
Payment Method: <%= @transaction.payment_method&.titleize || "N/A" %>
Date: <%= l(@transaction.created_at, format: :long) %>

Thank you for shopping with us!
