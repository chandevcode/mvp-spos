# fly.toml app configuration file generated for spos on 2026-08-06T14:13:42+08:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'spos'
primary_region = 'sin'
console_command = '/rails/bin/rails console'

[build]

[env]
  RAILS_MASTER_KEY = '40ee470d7278f311c6695f878c557597'
  PORT = '3000'
  TARGET_PORT = '3000'

[mounts]
  source = 'spos_storage'
  destination = '/rails/storage'

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

  [[http_service.checks]]
    interval = '10s'
    timeout = '2s'
    grace_period = '10s'
    method = 'GET'
    path = '/up'
    protocol = 'http'
    tls_skip_verify = false

    [http_service.checks.headers]
      X-Forwarded-Proto = 'https'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
